---
import BaseLayout from '../layouts/BaseLayout.astro';

export const prerender = true;
---

<BaseLayout
  title="Registro | Data Driven Day 2026"
  description="Crea tu cuenta en Data Driven Day y entra a tu tablero personal."
>
  <section class="section registro-shell">
    <div class="page-shell registro-page">
      <header class="registro-hero">
        <p class="section-label">Registro</p>
        <h1 class="registro-h1">Crea tu cuenta en Data Driven Day</h1>
        <p class="registro-intro">
          Regístrate, inicia sesión y entra a tu tablero para acceder a talleres, materiales,
          equipos, perfil y reconocimiento.
        </p>
      </header>

      <div class="registro-grid">
        <div class="registro-auth-column">
          <div id="authShell" class="registro-auth-card">
            <div class="registro-auth-tabs">
              <button class="registro-tab is-active" type="button" data-auth-tab="signup">Crear cuenta</button>
              <button class="registro-tab" type="button" data-auth-tab="login">Entrar</button>
            </div>

            <form id="signupForm" class="registro-auth-pane is-active" data-auth-pane="signup">
              <div class="registro-form-grid">
                <label class="registro-field registro-field--full">
                  <span class="registro-field-label">Nombre completo</span>
                  <input class="registro-input" type="text" name="fullName" placeholder="Tu nombre" required />
                </label>
                <label class="registro-field registro-field--full">
                  <span class="registro-field-label">Correo</span>
                  <input class="registro-input" type="email" name="email" placeholder="correo@ejemplo.com" required />
                </label>
                <label class="registro-field registro-field--full">
                  <span class="registro-field-label">Contraseña</span>
                  <input class="registro-input" type="password" name="password" placeholder="Mínimo 8 caracteres" minlength="8" required />
                </label>
              </div>

              <div id="signupStatus" class="registro-status" hidden></div>

              <div class="registro-auth-actions">
                <button class="button" type="submit">Crear cuenta</button>
              </div>
            </form>

            <form id="loginForm" class="registro-auth-pane" data-auth-pane="login">
              <div class="registro-form-grid registro-form-grid--login">
                <label class="registro-field registro-field--full">
                  <span class="registro-field-label">Correo</span>
                  <input class="registro-input" type="email" name="email" placeholder="correo@ejemplo.com" required />
                </label>
                <label class="registro-field registro-field--full">
                  <span class="registro-field-label">Contraseña</span>
                  <input class="registro-input" type="password" name="password" placeholder="Tu contraseña" required />
                </label>
              </div>

              <div id="loginStatus" class="registro-status" hidden></div>

              <div class="registro-auth-actions">
                <button class="button" type="submit">Entrar</button>
              </div>
            </form>
          </div>

          <div id="accountShell" class="registro-account-card" hidden>
            <p class="registro-account-kicker">Sesión activa</p>
            <h2 id="accountName" class="registro-account-title">Participante</h2>
            <p id="accountEmail" class="registro-account-copy"></p>
            <div id="accountFlags" class="registro-account-flags"></div>
            <div class="registro-auth-actions">
              <button id="logoutBtn" class="button-secondary" type="button">Cerrar sesión</button>
            </div>
          </div>
        </div>

        <div class="registro-dashboard-column">
          <section class="participant-shell">
            <header class="participant-topbar">
              <div>
                <p class="participant-topbar-label">Tablero</p>
                <h2 id="dashboardTitle" class="participant-topbar-title">Tu sesión de Data Driven Day</h2>
              </div>
              <div id="dashboardBadge" class="participant-topbar-badge">Sin sesión</div>
            </header>

            <div class="participant-content-grid">
              <section class="participant-panel participant-panel--wide">
                <p class="participant-panel-label">Materiales</p>
                <h3 class="participant-panel-title">Talleres y recursos</h3>
                <p id="resourceSummary" class="participant-panel-copy">
                  Inicia sesión para cargar tus materiales y recursos activos.
                </p>
                <div id="resourceList" class="participant-doc-list">
                  <div class="participant-doc-item">
                    <strong>Accede con tu cuenta para ver tus recursos</strong>
                  </div>
                </div>
              </section>

              <section class="participant-panel">
                <p class="participant-panel-label">Dataller</p>
                <h3 class="participant-panel-title">Activa tu participación</h3>
                <p id="datallerSummary" class="participant-panel-copy">
                  Inicia sesión para indicar que participas en el Dataller.
                </p>
                <div class="participant-lockbox participant-lockbox--compact">
                  <span id="datallerState" class="participant-lock-state">Sin sesión</span>
                  <p id="datallerCopy">Cuando actives Dataller, el admin podrá verte en ese grupo y liberar tu certificado.</p>
                  <div class="participant-inline-actions">
                    <button id="datallerToggleBtn" class="button-secondary" type="button" disabled>Activar Dataller</button>
                  </div>
                </div>
                <div id="datallerStatus" class="registro-status" hidden></div>
              </section>

              <section class="participant-panel">
                <p class="participant-panel-label">Equipos</p>
                <h3 class="participant-panel-title">Colabora con otros participantes</h3>
                <form id="teamForm" class="participant-form-grid">
                  <input class="registro-input" type="text" name="name" placeholder="Nombre del equipo" />
                  <input class="registro-input" type="text" name="focusArea" placeholder="Tema o interés" />
                  <textarea class="registro-input participant-textarea" name="description" placeholder="Descripción breve"></textarea>
                  <button class="button-secondary" type="submit">Crear equipo</button>
                </form>
                <div id="teamStatus" class="registro-status" hidden></div>
                <div class="participant-team-block">
                  <span class="participant-doc-tag">Mis equipos</span>
                  <div id="myTeamList" class="participant-mini-list">
                    <div class="participant-mini-row"><span>Sin sesión</span><span>--</span></div>
                  </div>
                </div>
                <div class="participant-team-block">
                  <span class="participant-doc-tag">Equipos abiertos</span>
                  <div id="openTeamList" class="participant-mini-list">
                    <div class="participant-mini-row"><span>Sin sesión</span><span>--</span></div>
                  </div>
                </div>
              </section>

              <section class="participant-panel">
                <p class="participant-panel-label">Reconocimiento</p>
                <h3 class="participant-panel-title">Tu estado</h3>
                <div id="recognitionBox" class="participant-lockbox">
                  <span id="recognitionState" class="participant-lock-state">Sin sesión</span>
                  <p id="recognitionCopy">Inicia sesión para ver el estado de tu reconocimiento.</p>
                  <div id="recognitionActions" class="participant-inline-actions participant-recognition-actions" hidden>
                    <a id="recognitionDownload" class="button-secondary" href="/api/participant/recognition" target="_blank" rel="noreferrer">Descargar PDF</a>
                    <span id="recognitionMeta" class="participant-doc-tag"></span>
                  </div>
                </div>
              </section>

              <section class="participant-panel participant-panel--wide">
                <p class="participant-panel-label">Perfil</p>
                <h3 class="participant-panel-title">Completa tu perfil</h3>
                <form id="profileForm" class="participant-form-grid participant-form-grid--profile">
                  <input class="registro-input" type="text" name="occupation" placeholder="Qué estudias o a qué te dedicas" />
                  <input class="registro-input" type="text" name="organization" placeholder="Escuela o trabajo" />
                  <input class="registro-input" type="url" name="projectUrl" placeholder="Enlace a tu proyecto" />
                  <input class="registro-input" type="url" name="avatarUrl" placeholder="URL de foto" />
                  <textarea class="registro-input participant-textarea participant-textarea--wide" name="bio" placeholder="Descripción corta"></textarea>
                  <button class="button-secondary" type="submit">Guardar perfil</button>
                </form>
                <div id="profileStatus" class="registro-status" hidden></div>
              </section>
            </div>
          </section>
        </div>
      </div>
    </div>
  </section>

  <script>
// @ts-nocheck
    const guestResources = [
      { titulo: 'Tus materiales aparecerán aquí cuando inicies sesión.' },
      { titulo: 'Tu tablero centraliza recursos, equipos y perfil.' },
    ];

    const tabs = document.querySelectorAll('[data-auth-tab]');
    const panes = document.querySelectorAll('[data-auth-pane]');
    const authShell = document.getElementById('authShell');
    const accountShell = document.getElementById('accountShell');
    const signupForm = document.getElementById('signupForm');
    const loginForm = document.getElementById('loginForm');
    const signupStatus = document.getElementById('signupStatus');
    const loginStatus = document.getElementById('loginStatus');
    const datallerStatus = document.getElementById('datallerStatus');
    const teamStatus = document.getElementById('teamStatus');
    const profileStatus = document.getElementById('profileStatus');
    const accountName = document.getElementById('accountName');
    const accountEmail = document.getElementById('accountEmail');
    const accountFlags = document.getElementById('accountFlags');
    const dashboardTitle = document.getElementById('dashboardTitle');
    const dashboardBadge = document.getElementById('dashboardBadge');
    const datallerSummary = document.getElementById('datallerSummary');
    const datallerState = document.getElementById('datallerState');
    const datallerCopy = document.getElementById('datallerCopy');
    const datallerToggleBtn = document.getElementById('datallerToggleBtn');
    const resourceSummary = document.getElementById('resourceSummary');
    const resourceList = document.getElementById('resourceList');
    const myTeamList = document.getElementById('myTeamList');
    const openTeamList = document.getElementById('openTeamList');
    const recognitionState = document.getElementById('recognitionState');
    const recognitionCopy = document.getElementById('recognitionCopy');
    const recognitionActions = document.getElementById('recognitionActions');
    const recognitionDownload = document.getElementById('recognitionDownload');
    const recognitionMeta = document.getElementById('recognitionMeta');
    const profileForm = document.getElementById('profileForm');
    const teamForm = document.getElementById('teamForm');
    const logoutBtn = document.getElementById('logoutBtn');

    const state = { participant: null };

    function escapeHtml(value) {
      return String(value ?? '')
        .replaceAll('&', '&amp;')
        .replaceAll('<', '&lt;')
        .replaceAll('>', '&gt;')
        .replaceAll('"', '&quot;')
        .replaceAll("'", '&#39;');
    }

    function setStatus(element, message, kind = 'info') {
      if (!element) return;
      if (!message) {
        element.hidden = true;
        element.textContent = '';
        element.className = 'registro-status';
        return;
      }
      element.hidden = false;
      element.textContent = message;
      element.className = `registro-status registro-status--${kind}`;
    }

    function renderResources(resources = []) {
      const items = resources.length ? resources : guestResources;
      resourceList.innerHTML = items.map((item) => `
        <div class="participant-doc-item">
          <strong>${escapeHtml(item.titulo)}</strong>
        </div>
      `).join('');
    }

    function renderTeams(myTeams = [], openTeams = [], loggedIn = false) {
      if (!loggedIn) {
        myTeamList.innerHTML = '<div class="participant-mini-row"><span>Sin sesión</span><span>--</span></div>';
        openTeamList.innerHTML = '<div class="participant-mini-row"><span>Sin sesión</span><span>--</span></div>';
        return;
      }

      myTeamList.innerHTML = myTeams.length
        ? myTeams.map((team) => `
            <div class="participant-mini-row participant-mini-row--stacked">
              <div>
                <strong>${escapeHtml(team.name)}</strong>
                <p>${escapeHtml(team.focus_area || team.description || 'Equipo')}</p>
              </div>
              <span>${team.member_count} personas</span>
            </div>
          `).join('')
        : '<div class="participant-mini-row"><span>No tienes equipos todavía</span><span>0</span></div>';

      openTeamList.innerHTML = openTeams.length
        ? openTeams.map((team) => `
            <div class="participant-mini-row participant-mini-row--stacked participant-mini-row--action">
              <div>
                <strong>${escapeHtml(team.name)}</strong>
                <p>${escapeHtml(team.focus_area || team.description || 'Equipo abierto')}</p>
              </div>
              <div class="participant-inline-actions">
                <span>${team.member_count} personas</span>
                <button class="button-secondary participant-join-btn" type="button" data-team-join="${escapeHtml(team.id)}">Unirme</button>
              </div>
            </div>
          `).join('')
        : '<div class="participant-mini-row"><span>No hay equipos abiertos</span><span>--</span></div>';
    }

    function renderDataller(participant) {
      if (!participant) {
        datallerSummary.textContent = 'Inicia sesión para indicar que participas en el Dataller.';
        datallerState.textContent = 'Sin sesión';
        datallerCopy.textContent = 'Cuando actives Dataller, el admin podrá verte en ese grupo y liberar tu certificado.';
        datallerToggleBtn.disabled = true;
        datallerToggleBtn.textContent = 'Activar Dataller';
        setStatus(datallerStatus, 'Inicia sesión para activar tu participación en Dataller.', 'info');
        return;
      }

      datallerToggleBtn.disabled = false;

      if (participant.datallerRegistered) {
        datallerSummary.textContent = 'Ya apareces como participante del Dataller.';
        datallerState.textContent = participant.workshopCompleted ? 'Validado' : 'Activo';
        datallerCopy.textContent = participant.recognitionEnabled
          ? 'Tu certificado ya fue autorizado por admin.'
          : 'Tu participación está visible para admin. Cuando te validen, podrán liberar tu certificado.';
        datallerToggleBtn.textContent = 'Salir de Dataller';
        setStatus(datallerStatus, participant.recognitionEnabled ? 'Tu certificado está autorizado.' : 'Tu registro de Dataller está activo.', participant.recognitionEnabled ? 'success' : 'info');
        return;
      }

      datallerSummary.textContent = 'Aún no activas tu participación en el Dataller.';
      datallerState.textContent = 'Inactivo';
      datallerCopy.textContent = 'Actívalo para que el admin pueda verte en el listado de Dataller y gestionar tu certificado.';
      datallerToggleBtn.textContent = 'Activar Dataller';
      setStatus(datallerStatus, 'Activa Dataller si participarás en ese taller.', 'warn');
    }

    function renderRecognition(participant) {
      recognitionActions.hidden = true;
      recognitionMeta.textContent = '';

      if (!participant) {
        recognitionState.textContent = 'Sin sesión';
        recognitionCopy.textContent = 'Inicia sesión para ver el estado de tu reconocimiento.';
        return;
      }

      if (!participant.datallerRegistered) {
        recognitionState.textContent = 'Dataller requerido';
        recognitionCopy.textContent = 'Activa primero tu participación en el Dataller para que admin pueda autorizar tu certificado.';
        return;
      }

      if (!participant.workshopCompleted) {
        recognitionState.textContent = 'Pendiente';
        recognitionCopy.textContent = 'Tu participación aún no ha sido validada.';
        return;
      }

      if (!participant.recognitionEnabled) {
        recognitionState.textContent = 'Validado';
        recognitionCopy.textContent = 'Tu reconocimiento aún no está liberado.';
        return;
      }

      recognitionState.textContent = 'Disponible';
      recognitionCopy.textContent = 'Tu reconocimiento está listo para descargar.';
      recognitionActions.hidden = false;
      recognitionDownload.href = '/api/participant/recognition';
      recognitionMeta.textContent = `Folio: ${participant.recognitionFolio || 'pendiente'}`;
    }

    function applyProfileState(participant) {
      const enabled = Boolean(participant && (participant.profileEnabled || participant.workshopCompleted));
      const fields = profileForm.querySelectorAll('input, textarea, button');
      fields.forEach((field) => {
        field.disabled = !enabled;
      });

      profileForm.occupation.value = participant?.occupation || '';
      profileForm.organization.value = participant?.organization || '';
      profileForm.projectUrl.value = participant?.projectUrl || '';
      profileForm.avatarUrl.value = participant?.avatarUrl || '';
      profileForm.bio.value = participant?.bio || '';

      if (!participant) {
        setStatus(profileStatus, 'Inicia sesión para editar tu perfil.', 'info');
      } else if (!enabled) {
        setStatus(profileStatus, 'Tu perfil aún no está habilitado.', 'warn');
      } else {
        setStatus(profileStatus, 'Tu perfil está listo para edición.', 'success');
      }
    }

    function renderAccount(participant) {
      accountName.textContent = participant.fullName;
      accountEmail.textContent = participant.email;
      accountFlags.innerHTML = [
        participant.datallerRegistered ? 'En Dataller' : 'Sin Dataller',
        participant.workshopCompleted ? 'Validado' : 'Pendiente',
        participant.profileEnabled ? 'Perfil habilitado' : 'Perfil bloqueado',
        participant.recognitionEnabled ? 'Reconocimiento activo' : 'Reconocimiento pendiente',
      ].map((label) => `<span class="participant-status-chip participant-status-chip--muted">${label}</span>`).join('');
    }

    function renderLoggedOut() {
      state.participant = null;
      authShell.hidden = false;
      accountShell.hidden = true;
      dashboardTitle.textContent = 'Tu sesión de Data Driven Day';
      dashboardBadge.textContent = 'Sin sesión';
      renderDataller(null);
      resourceSummary.textContent = 'Inicia sesión para cargar tus materiales y recursos activos.';
      renderResources();
      renderTeams([], [], false);
      renderRecognition(null);
      applyProfileState(null);
      setStatus(teamStatus, 'Inicia sesión para gestionar equipos.', 'info');
    }

    async function loadDashboard() {
      const response = await fetch('/api/participant/dashboard');
      if (!response.ok) {
        throw new Error('dashboard_failed');
      }

      const data = await response.json();
      state.participant = data.participant;
      authShell.hidden = true;
      accountShell.hidden = false;
      renderAccount(data.participant);
      dashboardTitle.textContent = `Hola, ${data.participant.fullName.split(' ')[0]}`;
      dashboardBadge.textContent = 'Sesión activa';
      resourceSummary.textContent = data.recursos?.length
        ? `${data.recursos.length} recursos activos disponibles.`
        : 'No hay recursos activos todavía.';
      renderResources(data.recursos || []);
      renderDataller(data.participant);
      renderTeams(data.myTeams || [], data.openTeams || [], true);
      renderRecognition(data.participant);
      applyProfileState(data.participant);
      setStatus(teamStatus, '', 'info');
    }

    async function restoreSession() {
      try {
        const response = await fetch('/api/participant/me');
        if (!response.ok) {
          renderLoggedOut();
          return;
        }
        await loadDashboard();
      } catch {
        renderLoggedOut();
      }
    }

    tabs.forEach((tab) => {
      tab.addEventListener('click', () => {
        const view = tab.getAttribute('data-auth-tab');
        tabs.forEach((item) => item.classList.toggle('is-active', item === tab));
        panes.forEach((pane) => pane.classList.toggle('is-active', pane.getAttribute('data-auth-pane') === view));
      });
    });

    signupForm.addEventListener('submit', async (event) => {
      event.preventDefault();
      setStatus(signupStatus, 'Creando cuenta...', 'info');

      const payload = {
        fullName: signupForm.fullName.value.trim(),
        email: signupForm.email.value.trim(),
        password: signupForm.password.value,
      };

      try {
        const response = await fetch('/api/participant/signup', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload),
        });
        const data = await response.json();

        if (!response.ok) {
          const messages = {
            missing_fields: 'Faltan campos obligatorios.',
            weak_password: 'La contraseña debe tener al menos 8 caracteres.',
            email_taken: 'Ese correo ya tiene cuenta.',
            invalid_email: 'Escribe un correo valido.',
          };
          setStatus(signupStatus, messages[data.error] || 'No pude crear la cuenta.', 'error');
          return;
        }

        signupForm.reset();
        setStatus(signupStatus, 'Cuenta creada. Cargando tu tablero...', 'success');
        await loadDashboard();
      } catch {
        setStatus(signupStatus, 'Error de red al crear la cuenta.', 'error');
      }
    });

    loginForm.addEventListener('submit', async (event) => {
      event.preventDefault();
      setStatus(loginStatus, 'Entrando...', 'info');

      try {
        const response = await fetch('/api/participant/login', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            email: loginForm.email.value.trim(),
            password: loginForm.password.value,
          }),
        });
        const data = await response.json();

        if (!response.ok) {
          const message = data.error === 'invalid_credentials'
            ? 'Correo o contraseña incorrectos.'
            : data.error === 'invalid_email'
              ? 'Escribe un correo valido.'
              : 'No pude abrir la sesión.';
          setStatus(loginStatus, message, 'error');
          return;
        }

        loginForm.reset();
        setStatus(loginStatus, 'Sesión abierta. Cargando tu tablero...', 'success');
        await loadDashboard();
      } catch {
        setStatus(loginStatus, 'Error de red al iniciar sesión.', 'error');
      }
    });

    logoutBtn.addEventListener('click', async () => {
      await fetch('/api/participant/logout', { method: 'POST' }).catch(() => null);
      setStatus(signupStatus, '', 'info');
      setStatus(loginStatus, '', 'info');
      renderLoggedOut();
    });

    datallerToggleBtn.addEventListener('click', async () => {
      if (!state.participant) {
        setStatus(datallerStatus, 'Necesitas iniciar sesión primero.', 'warn');
        return;
      }

      const nextValue = !state.participant.datallerRegistered;
      setStatus(datallerStatus, nextValue ? 'Activando Dataller...' : 'Saliendo de Dataller...', 'info');

      try {
        const response = await fetch('/api/participant/dataller', {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ datallerRegistered: nextValue }),
        });
        const data = await response.json();

        if (!response.ok) {
          setStatus(datallerStatus, 'No pude actualizar tu estado de Dataller.', 'error');
          return;
        }

        state.participant = data.participant;
        renderAccount(data.participant);
        renderDataller(data.participant);
        renderRecognition(data.participant);
      } catch {
        setStatus(datallerStatus, 'Error de red al actualizar Dataller.', 'error');
      }
    });

    teamForm.addEventListener('submit', async (event) => {
      event.preventDefault();
      if (!state.participant) {
        setStatus(teamStatus, 'Necesitas iniciar sesión para crear un equipo.', 'warn');
        return;
      }

      setStatus(teamStatus, 'Creando equipo...', 'info');
      try {
        const response = await fetch('/api/participant/teams', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            name: teamForm.name.value.trim(),
            focusArea: teamForm.focusArea.value.trim(),
            description: teamForm.description.value.trim(),
          }),
        });
        const data = await response.json();

        if (!response.ok) {
          setStatus(teamStatus, data.error === 'missing_name' ? 'Ponle nombre al equipo.' : 'No pude crear el equipo.', 'error');
          return;
        }

        teamForm.reset();
        setStatus(teamStatus, 'Equipo creado.', 'success');
        await loadDashboard();
      } catch {
        setStatus(teamStatus, 'Error de red al crear el equipo.', 'error');
      }
    });

    openTeamList.addEventListener('click', async (event) => {
      const button = event.target.closest('[data-team-join]');
      if (!button || !state.participant) return;

      setStatus(teamStatus, 'Uniéndote al equipo...', 'info');
      try {
        const response = await fetch(`/api/participant/teams/${button.dataset.teamJoin}/join`, {
          method: 'POST',
        });
        if (!response.ok) {
          setStatus(teamStatus, 'No pude unirte a ese equipo.', 'error');
          return;
        }
        setStatus(teamStatus, 'Te uniste al equipo.', 'success');
        await loadDashboard();
      } catch {
        setStatus(teamStatus, 'Error de red al unirte al equipo.', 'error');
      }
    });

    profileForm.addEventListener('submit', async (event) => {
      event.preventDefault();
      if (!state.participant) {
        setStatus(profileStatus, 'Necesitas iniciar sesión primero.', 'warn');
        return;
      }

      setStatus(profileStatus, 'Guardando perfil...', 'info');
      try {
        const response = await fetch('/api/participant/profile', {
          method: 'PATCH',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            occupation: profileForm.occupation.value.trim(),
            organization: profileForm.organization.value.trim(),
            projectUrl: profileForm.projectUrl.value.trim(),
            avatarUrl: profileForm.avatarUrl.value.trim(),
            bio: profileForm.bio.value.trim(),
          }),
        });
        const data = await response.json();

        if (!response.ok) {
          const message = data.error === 'profile_locked'
            ? 'Tu perfil aun no esta habilitado.'
            : data.error === 'invalid_project_url'
              ? 'El enlace del proyecto debe iniciar con http:// o https://.'
              : data.error === 'invalid_avatar_url'
                ? 'La URL de foto debe iniciar con http:// o https://.'
                : 'No pude guardar el perfil.';
          setStatus(profileStatus, message, 'error');
          return;
        }

        state.participant = data.participant;
        renderAccount(data.participant);
        renderRecognition(data.participant);
        applyProfileState(data.participant);
        setStatus(profileStatus, 'Perfil guardado.', 'success');
      } catch {
        setStatus(profileStatus, 'Error de red al guardar el perfil.', 'error');
      }
    });

    renderLoggedOut();
    restoreSession();
  </script>

  <style>
    .registro-shell {
      padding-top: 4.5rem;
    }
    .registro-page {
      display: grid;
      gap: 2rem;
    }
    .registro-hero {
      max-width: 760px;
    }
    .registro-h1 {
      margin: 0 0 0.9rem;
      font-family: var(--display);
      font-size: clamp(2.2rem, 5vw, 4rem);
      line-height: 0.96;
      letter-spacing: -0.04em;
    }
    .registro-intro {
      margin: 0;
      color: var(--color-text-muted);
      font-size: 1rem;
      line-height: 1.8;
      max-width: 58ch;
    }
    .registro-grid {
      display: grid;
      grid-template-columns: minmax(340px, 420px) minmax(0, 1fr);
      gap: 1.5rem;
      align-items: start;
    }
    .registro-auth-column {
      display: grid;
      gap: 1rem;
      position: sticky;
      top: 6rem;
    }
    .registro-auth-card,
    .registro-account-card,
    .participant-shell {
      border: 1px solid var(--color-border);
      background: linear-gradient(180deg, rgba(22,27,34,0.98), rgba(13,17,23,0.98));
      border-radius: 1rem;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.22);
    }
    .registro-auth-card,
    .registro-account-card {
      padding: 1.25rem;
    }
    .registro-auth-tabs {
      display: inline-grid;
      grid-auto-flow: column;
      gap: 0.25rem;
      padding: 0.25rem;
      border-radius: 999px;
      background: rgba(255,255,255,0.04);
      border: 1px solid var(--color-border-soft);
      margin-bottom: 1rem;
    }
    .registro-tab {
      border: 0;
      background: transparent;
      color: var(--color-text-muted);
      padding: 0.55rem 0.9rem;
      border-radius: 999px;
      font-family: var(--mono);
      font-size: 0.72rem;
      cursor: pointer;
    }
    .registro-tab.is-active {
      background: var(--color-accent-blue);
      color: #fff;
    }
    .registro-auth-pane {
      display: none;
    }
    .registro-auth-pane.is-active {
      display: block;
    }
    .registro-form-grid {
      display: grid;
      gap: 0.85rem;
    }
    .registro-form-grid--login {
      max-width: none;
    }
    .registro-field {
      display: grid;
      gap: 0.35rem;
    }
    .registro-field--full {
      grid-column: 1 / -1;
    }
    .registro-field-label,
    .registro-account-kicker,
    .participant-topbar-label,
    .participant-panel-label,
    .participant-doc-tag {
      font-family: var(--mono);
      font-size: 0.64rem;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: var(--color-accent-sky);
    }
    .registro-input {
      width: 100%;
      border: 1px solid var(--color-border-soft);
      border-radius: 0.75rem;
      background: rgba(12,18,30,0.78);
      color: var(--color-text-primary);
      padding: 0.85rem 0.95rem;
      font: inherit;
      transition: border-color 0.18s, box-shadow 0.18s;
    }
    .registro-input:focus {
      outline: none;
      border-color: rgba(96,165,250,0.55);
      box-shadow: 0 0 0 3px rgba(59,130,246,0.16);
    }
    .registro-auth-actions {
      display: flex;
      gap: 0.75rem;
      flex-wrap: wrap;
      margin-top: 1rem;
    }
    .registro-status {
      margin-top: 0.9rem;
      padding: 0.75rem 0.9rem;
      border-radius: 0.75rem;
      border: 1px solid var(--color-border-soft);
      color: var(--color-text-secondary);
      background: rgba(255,255,255,0.03);
      line-height: 1.5;
    }
    .registro-status--success {
      border-color: rgba(52,211,153,0.4);
      color: rgb(167,243,208);
    }
    .registro-status--warn {
      border-color: rgba(251,191,36,0.35);
      color: rgb(253,230,138);
    }
    .registro-status--error {
      border-color: rgba(248,113,113,0.4);
      color: rgb(254,202,202);
    }
    .registro-account-title {
      margin: 0.35rem 0 0.2rem;
      font-size: 1.4rem;
    }
    .registro-account-copy {
      margin: 0;
      color: var(--color-text-muted);
    }
    .registro-account-flags {
      display: flex;
      gap: 0.5rem;
      flex-wrap: wrap;
      margin-top: 0.9rem;
    }
    .participant-shell {
      overflow: hidden;
    }
    .participant-topbar {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 1rem;
      padding: 1rem 1.25rem;
      border-bottom: 1px solid var(--color-border);
    }
    .participant-topbar-title {
      margin: 0.15rem 0 0;
      font-size: 1.25rem;
    }
    .participant-topbar-badge,
    .participant-status-chip {
      display: inline-flex;
      align-items: center;
      width: fit-content;
      padding: 0.35rem 0.65rem;
      border-radius: 999px;
      font-family: var(--mono);
      font-size: 0.64rem;
      border: 1px solid var(--color-border-soft);
      color: var(--color-text-muted);
    }
    .participant-status-chip--muted {
      border-color: var(--color-border-soft);
      color: var(--color-text-muted);
    }
    .participant-content-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
      padding: 1.25rem;
    }
    .participant-panel {
      display: grid;
      gap: 0.8rem;
      min-width: 0;
      padding: 1rem;
      border: 1px solid var(--color-border);
      border-radius: 0.9rem;
      background: rgba(255,255,255,0.02);
    }
    .participant-panel--wide {
      grid-column: 1 / -1;
    }
    .participant-panel-title {
      margin: 0;
      font-size: 1.05rem;
    }
    .participant-panel-copy {
      margin: 0;
      color: var(--color-text-muted);
      line-height: 1.7;
    }
    .participant-doc-list,
    .participant-mini-list {
      display: grid;
      gap: 1px;
      background: var(--color-border);
      border: 1px solid var(--color-border);
    }
    .participant-doc-item,
    .participant-mini-row {
      display: flex;
      justify-content: space-between;
      gap: 1rem;
      align-items: center;
      padding: 0.9rem 1rem;
      background: var(--color-bg);
    }
    .participant-mini-row--stacked {
      align-items: flex-start;
    }
    .participant-mini-row--stacked p {
      margin: 0.35rem 0 0;
      color: var(--color-text-muted);
      line-height: 1.6;
    }
    .participant-inline-actions {
      display: flex;
      align-items: center;
      gap: 0.75rem;
      flex-wrap: wrap;
    }
    .participant-form-grid {
      display: grid;
      gap: 0.75rem;
    }
    .participant-form-grid--profile {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
    .participant-textarea {
      min-height: 110px;
      resize: vertical;
    }
    .participant-textarea--wide {
      grid-column: 1 / -1;
    }
    .participant-team-block {
      display: grid;
      gap: 0.45rem;
    }
    .participant-lockbox {
      display: flex;
      flex-direction: column;
      align-items: flex-start;
      gap: 0.8rem;
      padding: 1rem;
      border-radius: 0.8rem;
      background: rgba(13,17,23,0.72);
      border: 1px dashed var(--color-border-soft);
      color: var(--color-text-muted);
    }
    .participant-lockbox--compact {
      min-height: 100%;
    }
    .participant-lockbox p {
      margin: 0;
      line-height: 1.6;
    }
    .participant-lock-state {
      font-family: var(--mono);
      font-size: 0.66rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--color-accent-warn);
    }
    @media (max-width: 1080px) {
      .registro-grid {
        grid-template-columns: 1fr;
      }
      .registro-auth-column {
        position: static;
      }
    }
    @media (max-width: 780px) {
      .participant-content-grid,
      .participant-form-grid--profile {
        grid-template-columns: 1fr;
      }
      .participant-topbar {
        align-items: flex-start;
        flex-direction: column;
      }
    }
  </style>
</BaseLayout>